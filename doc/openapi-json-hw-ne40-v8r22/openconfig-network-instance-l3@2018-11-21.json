{
    "swagger": "2.0",
    "info": {
        "description": "openconfig-network-instance-l3 API generated from openconfig-network-instance-l3@2018-11-21.yang",
        "version": "1.0.0",
        "title": "openconfig-network-instance-l3 API"
    },
    "host": "localhost:8080",
    "basePath": "/restconf",
    "schemes": [
        "http"
    ],
    "paths": {},
    "definitions": {
        "l3ni-instance": {
            "description": "Configuration and operational state parameters relevant\nto network instances that include a Layer 3 type"
        },
        "l3ni-instance-common-config": {
            "description": "Configuration parameters that are common to L3 network\ninstances other than the default instance",
            "properties": {
                "enabled-address-families": {
                    "type": "array",
                    "items": {
                        "type": "string",
                        "description": "The address families that are to be enabled for this\nnetwork instance."
                    }
                }
            }
        },
        "l3ni-route-limit-structural": {
            "description": "Configuration and state for the maximum number of routes\nthat should be used by routing instance.",
            "properties": {
                "route-limits": {
                    "description": "Configuration and operational state relating to the\nmaximum number of routes for the address family that\nshould be allowed within the Layer 3 network instance.\n\nWhen the specified value is reached, no further prefixes\nshould be installed into the system's RIB from this network\ninstance unless the warning only leaf is set. In this case,\nnew routes should still be installed. If a alarm threshold\nis specified, then this should be used to generate\nalarms via telemetry for the network instance.",
                    "properties": {
                        "route-limit": {
                            "description": "A route limit applying to a particular address family.",
                            "type": "array",
                            "items": {
                                "properties": {
                                    "afi": {
                                        "type": "string",
                                        "x-path": "../config/afi",
                                        "description": "Reference to the address family for which the route\nlimit is being applied."
                                    },
                                    "config": {
                                        "description": "Configuration options relating to the route limit.",
                                        "$ref": "#/definitions/l3ni-route-limit-config"
                                    },
                                    "state": {
                                        "description": "Operational state parameters relating to the route limit.",
                                        "$ref": "#/definitions/l3ni-route-limit-state"
                                    }
                                }
                            },
                            "x-key": "afi"
                        }
                    }
                }
            }
        },
        "l3ni-route-limit-config": {
            "description": "Configuration options relating to the route limit for a network\ninstance.",
            "properties": {
                "afi": {
                    "type": "string",
                    "description": "The address family for which the route limit applies."
                },
                "maximum": {
                    "type": "string",
                    "description": "The maximum number of routes for the address family. The\nsystem should not install more than maximum number of\nprefixes into the RIB unless the warning-only leaf is specified."
                },
                "warning-only": {
                    "type": "boolean",
                    "description": "When specified, the route limit specified is considered only as\na warning - and routes should continue to be installed into the\nRIB over the limit specified in the maximum leaf."
                },
                "alarm-threshold": {
                    "type": "string",
                    "description": "When specified, an alarm should be generated when the threshold\nnumber of installed routes is reached."
                }
            }
        },
        "l3ni-route-limit-state": {
            "description": "Operational state relating to the route limit for a network\ninstance.",
            "properties": {
                "threshold-exceeded": {
                    "type": "boolean",
                    "description": "This leaf should be set to true in the case that the threshold\nnumber of routes has been exceeded."
                },
                "installed-routes": {
                    "type": "string",
                    "description": "The current number of routes installed for the address family."
                }
            }
        }
    }
}